#include <stdio.h>

#include "utils.h"

int main() {
    int a = 0;
    int b = 0;

    // Prompt the user to enter two integers

    // Call `swap_int`, provided in `utils.h` to swap the values.

    printf("a = %d, b = %d\n", a, b);

    return 0;
}
